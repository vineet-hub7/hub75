# Simulation build artifacts (regenerated by `make` / run_sim.ps1)
*.vvp
*.vcd

# Generated test images (image_8x8.hex is checked in; others are generated)
sim/img_*.hex
sim/image_16x16.hex
sim/image_32x16.hex

# OS / editor cruft
.DS_Store
Thumbs.db

# Local Claude Code settings (machine-specific)
.claude/
